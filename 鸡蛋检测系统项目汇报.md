# 🥚 基于计算机视觉的鸡蛋在线追踪与计数系统汇报

## 1. 项目背景与目标
*   **研发背景**：在现代化的家禽养殖和蛋品加工流线中，人工计数或简单的机械计数不仅效率低下，且容易在密集传送中出现漏算、误算等问题。为了实现生产线的自动化与数字化监控，我们需要一种高吞吐、高精度的无接触式视觉计数方案。
*   **项目目标**：复用成熟的工业级视觉追踪底座，将原有的“猪只多目标追踪系统”自适应扩展为“鸡蛋流水线在线检测与追踪计数系统”，实现对传送带或生产线上高密度移动鸡蛋的精确识别、轨迹追踪及自动化数量统计。

## 2. 核心技术架构 (技术栈)
本项目依托于最前沿的深度学习核心技术，放弃了传统的物理传感器，完全依靠摄像头视觉算法实现：
*   **目标检测引擎 (Detection)**：使用了 **RF-DETR (Real-Time DEtection TRansformer)**。
    *   *技术优势*：作为基于 Transformer 架构的实时检测器，它无需繁琐的 NMS（非极大值抑制）后处理。对于传送带上极其密集且呈各种角度堆叠分布的鸡蛋，RF-DETR 能提供无与伦比的检测精细度。
*   **多目标追踪方案 (Tracking)**：仅采用纯运动学追踪算法 **ByteTrack**。
    *   *技术优势*：由于鸡蛋个体之间的外观极其相似（缺乏足够的视觉特征区分度），诸如 DeepSORT 这种依赖外观特征提取器 (ReID) 的算法会大打折扣且徒增计算压力。ByteTrack 大道至简，将低置信度检测框也纳入卡尔曼滤波 (Kalman Filter) 关联，完美解决了鸡蛋在传送带上相互遮挡时的 ID 跳变问题。
*   **底层基础建设**：使用 **PyTorch** 提供深度学习计算，结合 **OpenCV** 进行高速的流媒体帧解析与渲染，依靠 **Supervision** 框架处理空间逻辑规划与可视化。

## 3. 核心业务工作流 ( Workflow )
整个鸡蛋计数的流线是一个帧级别 (Frame-by-frame) 的高速 Pipeline：
1.  **视频流获取**：通过工业相机获取传送带视频源。
2.  **高精度抓取**：RF-DETR 对每一帧图像进行推理，精准框出所有鸡蛋像素级位置。
3.  **身份分配与轨迹预测 (ByteTrack)**：为每一个在画面中新出现的鸡蛋分配专属 ID，通过卡尔曼滤波预测其次秒的运动轨迹，实现帧与帧之间同一个鸡蛋的平移拼图。
4.  **动态越线计数**：在视频流空间中划定虚拟检测线（例如基于原有系统的三线防抖校验机制：`Line 0`, `Line 1`, `Line 2`）。当某个拥有专属 ID 的鸡蛋运动轨迹穿过虚拟线时，系统自动拦截信号，计数器 +1。
5.  **数据落盘**：实时渲染标记后的视频记录，并输出 CSV 维度的流转日志（包含入场时间、越线数量、统计区间等）。

## 4. 针对鸡蛋场景的特定调优与优化点
为了从“追踪大型目标”向“追踪传送带鸡蛋”转变，我们在系统中执行了以下针对性改良：
*   **特征解耦与模型微调**：收集并标注了海量不同光照条件、不同遮挡情况下的鸡蛋数据集，对原有的 RF-DETR 模型进行了重新微调 (Fine-tuning)，使其专门适应椭圆形目标的检测。
*   **摒弃 ReID 模块**：在追踪器的选择上，斩钉截铁地去掉了所有复杂特征提取追踪器，**只保留了 ByteTrack**。这直接极大地降低了 GPU 显存占用，使得流水线的处理帧率 (FPS) 得到了跨越式提升。
*   **防抖动计数逻辑调整**：由于鸡蛋体量较小且运动轨迹更接近于单向平移直线（传送带），我们重新标定了检测分隔线的阈值参数 (`out_ratio` / `wait_ratio`)。采用多条线（三线交叉）统计，将穿越总次数取平均计算，有效过滤了传送带倒转、轻微震动带来的重复计录误差。

## 5. 项目成果与未来展望
*   **阶段性成果**：系统目前可在常规 GPU 设备上实现极高帧率的实时检测，错报、漏报率大幅降低。利用 ByteTrack 和多线滤波，基本消除了高密度堆叠情况下的 ID 混淆。
*   **未来扩展延伸 (To-Do)**：
    1.  **瑕疵检测集成**：在鸡蛋已被成功追踪捕捉的前提下，可以后期串接一个小型的分类网络，同步检测是否存在“破损蛋”、“脏污蛋”。
    2.  **端侧部署 (Edge AI)**：未来计划使用 ONNX Runtime / TensorRT 对模型进行量化压缩，使其可以直接运行在传送带侧边的廉价树莓派或 Jetson Nano 边缘计算盒子上。
