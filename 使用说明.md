# 猪只计数系统使用说明

## 项目概述

基于 RF-DETR 目标检测 + ByteTrack 多目标跟踪的猪只自动计数系统，支持两种运行模式：
- **Windows PC**：使用 GPU 对本地视频批量处理
- **Atlas 200I DK A2**：使用华为昇腾 NPU 对摄像头实时推理或本地视频批量处理

---

## 一、项目文件结构

```
pig_tracking_system/
├── models/
│   └── checkpoint_best_ema.pth          # RF-DETR 训练权重
├── input_videos/                         # 待处理视频（Windows 批处理用）
├── output/                               # Windows 批处理输出结果
├── scripts/
│   ├── compare_trackers.py              # Windows 推理主脚本
│   └── batch_process_videos.py          # Windows 批量处理脚本
├── deploy/
│   ├── export_onnx.py                   # 导出 ONNX 模型
│   ├── fix_onnx.py                      # 修复 TopK 融合（ATC 兼容）
│   ├── fix_topk_k.py                    # 修复 TopK k 类型（INT64→INT32）
│   ├── atlas_infer.py                   # Atlas NPU 推理封装（pyACL）
│   ├── atlas_pipeline.py                # Atlas 实时/视频推理主脚本
│   ├── atlas_batch.py                   # Atlas 批量处理脚本
│   └── resource_monitor.py              # NPU/内存资源监控
├── third_party/
│   └── OC_SORT/
│       └── trackers/
│           └── byte_tracker/            # ByteTrack 跟踪器
└── 使用说明.md
```

---

## 二、Windows PC 使用（GPU 推理）

### 环境要求

- Python 环境：`F:\conda\envs\rf-detr`（PyTorch 2.5.1 + CUDA 12.1）
- 激活环境：`conda activate rf-detr`

### 2.1 处理单个视频

```bash
cd F:\code1.0\pig_tracking_system
conda activate rf-detr

python scripts/compare_trackers.py \
    --video_path "F:\code1.0\pig_tracking_system\input_videos\视频文件名.mp4" \
    --model_path "F:\code1.0\pig_tracking_system\models\checkpoint_best_ema.pth" \
    --output_dir "F:\code1.0\pig_tracking_system\output" \
    --tracker ByteTrack
```

### 2.2 批量处理所有视频

```bash
python scripts/batch_process_videos.py
```

自动处理 `input_videos/` 目录下所有 mp4 文件，结果保存至 `output/`。

### 2.3 常用参数

| 参数 | 默认值 | 说明 |
|---|---|---|
| `--tracker` | ByteTrack | 跟踪器类型（ByteTrack / DeepSORT_ReID / SimpleIOU） |
| `--out_ratio` | 0.45 | OUT 区占画面宽度比例 |
| `--wait_ratio` | 0.25 | WAIT 区占画面宽度比例 |
| `--limit` | 0 | 最大处理帧数，0 不限制 |

---

## 三、Atlas 200I DK A2 部署

### 硬件规格

| 项目 | 参数 |
|---|---|
| 芯片型号 | Ascend 310B1 |
| AI 算力（INT8） | 20 TOPS |
| NPU 内存 | 11.3 GB |
| AI 架构 | 华为达芬奇（DaVinci） |

### 硬件连接

```
大华摄像头（192.168.2.103）──网线──▶ Atlas eth0（192.168.2.200）
Windows PC（192.168.137.x）  ──网线──▶ Atlas eth1（192.168.137.100）
```

### Atlas 上的模型文件

| 文件 | 说明 |
|---|---|
| `/root/pig_tracking/inference_model.onnx` | 原始导出 ONNX（102.9 MB） |
| `/root/pig_tracking/inference_model_fixed2.onnx` | 修复后的 ONNX（ATC 兼容） |
| `/root/pig_tracking/inference_model_310B1.om` | 针对 310B1 编译的 OM 模型（推荐使用） |
| `/root/pig_tracking/fusion_switch.cfg` | 关闭融合优化配置（规避 CANN bug，需手动创建） |

> `fusion_switch.cfg` 不在项目代码中，需在 Atlas 上手动创建，见第九章 9.3 节。

---

## 四、Atlas 使用教程

### 4.1 首次启动配置（每次重启后需执行）

SSH 登录 Atlas：
```bash
ssh root@192.168.137.100
```

配置摄像头网络：
```bash
ip addr flush dev eth0
ip addr add 192.168.2.200/24 dev eth0
ip link set eth0 up

# 验证摄像头可达
ping -c 4 192.168.2.103
```

### 4.2 实时摄像头计数

```bash
cd /root/pig_tracking
python deploy/atlas_pipeline.py \
    --rtsp "rtsp://admin:admin123%40@192.168.2.103:554/cam/realmonitor?channel=1&subtype=0" \
    --om /root/pig_tracking/inference_model_310B1.om \
    --output /root/pig_tracking/output \
    --no_video
```

按 **Ctrl+C** 停止，自动生成计数报告。

### 4.3 处理本地视频文件

```bash
cd /root/pig_tracking
python deploy/atlas_pipeline.py \
    --rtsp /root/pig_tracking/片段/视频文件名.mp4 \
    --om /root/pig_tracking/inference_model_310B1.om \
    --output /root/pig_tracking/output \
    --no_video
```

### 4.4 批量处理视频目录

```bash
cd /root/pig_tracking
python deploy/atlas_batch.py \
    --video_dir /root/pig_tracking/片段 \
    --om /root/pig_tracking/inference_model_310B1.om \
    --output /root/pig_tracking/output \
    --no_video
```

### 4.5 常用参数

| 参数 | 默认值 | 说明 |
|---|---|---|
| `--conf` | 0.5 | 检测置信度，越高漏检越多但误检越少 |
| `--out_ratio` | 0.45 | OUT 区占画面宽度比例 |
| `--wait_ratio` | 0.25 | WAIT 区占画面宽度比例 |
| `--no_video` | 关 | 不保存标注视频，只输出 CSV 报告 |
| `--no_monitor` | 关 | 关闭资源占用监控 |
| `--max_frames` | 0 | 最大处理帧数，0 不限制 |

---

## 五、计数结果说明

每次运行后在 `--output` 目录生成三个文件：

| 文件 | 内容 |
|---|---|
| `ByteTrack_Atlas_state_changes.txt` | 每只猪的轨迹详情，**末尾为最终计数** |
| `ByteTrack_Atlas_trajectory_report.csv` | 每条轨迹汇总，末尾有 FINAL_COUNT 行 |
| `ByteTrack_Atlas_id_events.csv` | 每个 ID 的区域变化事件记录 |

### 计数逻辑

画面从右到左划分三个区域：

```
|  ENTRY(30%)  |  WAIT(25%)  |     OUT(45%)      |
               ↑             ↑         ↑
             Line2         Line1     Line0
```

- 三条线各自统计从 ENTRY 方向穿越的猪只数
- **最终计数 = 三条线计数的平均值向下取整**

`state_changes.txt` 末尾示例：
```
Line0: 45  Line1: 46  Line2: 47  均值: 46.00
FINAL COUNT (三线均值向下取整): 46
TOTAL IDs: 58
```

### 把结果拷回 Windows

```powershell
scp -r root@192.168.137.100:/root/pig_tracking/output "F:\atlas_results\"
```

---

## 六、资源监控

运行时每 10 秒自动打印：
```
[Monitor] Frame    300 | NPU算力=62%  NPU显存=1024/11577MB  系统内存=1856MB
```

结束时输出汇总：
```
====================================================
资源使用汇总
====================================================
  NPU 算力    均值:  60.3%   峰值:  78.0%
  NPU 显存    均值:  1024 MB  峰值:  1024 MB  / 11577 MB
  系统内存    均值:  1860 MB  峰值:  1920 MB
====================================================
```

---

## 七、ONNX 模型重新编译（如需更换芯片版本）

在 Atlas 上执行：
```bash
atc --model=/root/pig_tracking/inference_model_fixed2.onnx \
    --framework=5 \
    --output=/root/pig_tracking/inference_model_310B1 \
    --soc_version=Ascend310B1 \
    --fusion_switch_file=/root/pig_tracking/fusion_switch.cfg \
    --input_shape="input:1,3,560,560" \
    --log=warning
```

---

## 八、常见问题

### Q：RTSP 连接失败
- 检查 eth0 是否配置了正确 IP：`ip addr show eth0`
- 检查摄像头是否可达：`ping 192.168.2.103`
- 密码中的 `@` 在 RTSP URL 中需写成 `%40`

### Q：推理结果计数异常偏高或偏低
- 调整置信度：`--conf 0.6`（减少误检）或 `--conf 0.4`（减少漏检）
- 调整区域比例：`--out_ratio` 和 `--wait_ratio` 需根据实际摄像头角度调整

### Q：pip install 找不到包
```bash
pip install opencv-python-headless scipy -i https://repo.huaweicloud.com/repository/pypi/simple
```

### Q：重启后摄像头断连
Atlas 重启后 eth0 的 IP 配置会丢失，需手动恢复：
```bash
ip addr flush dev eth0
ip addr add 192.168.2.200/24 dev eth0
ip link set eth0 up
ping -c 3 192.168.2.103
```

开机自动配置见第九章 9.7 节（使用 rc.local 方案）。

---

## 九、换新 SSD 后重新部署教程

### 9.1 前提条件

- Windows PC 已通过网线连接 Atlas eth1（Atlas IP：192.168.137.100）
- Atlas 已正常开机，可以 SSH 登录
- 项目代码在 Windows 上的路径：`F:\code1.0\pig_tracking_system`
- 模型文件已准备好（见下方文件清单）

### 9.2 需要上传的文件清单

| Windows 本地路径 | 上传到 Atlas 路径 | 说明 |
|---|---|---|
| `F:\code1.0\pig_tracking_system\`（整个目录） | `/root/pig_tracking/` | 项目代码 |
| `F:\...\inference_model_fixed2.onnx` | `/root/pig_tracking/` | 修复后的 ONNX 模型 |

> `fusion_switch.cfg` 不在 Windows 上，需在 Atlas 上直接创建（见 9.3 节）。
> OM 模型（.om 文件）需在 Atlas 上重新用 ATC 编译生成，不能直接拷贝（与芯片版本绑定）。

### 9.3 在 Atlas 上创建 fusion_switch.cfg

SSH 登录 Atlas 后执行：

```bash
mkdir -p /root/pig_tracking
cat > /root/pig_tracking/fusion_switch.cfg << 'EOF'
{
    "Switch":{
        "GraphFusion":{
            "ALL":"off"
        }
    }
}
EOF
```

**作用：** 关闭 ATC 图融合优化，规避 CANN 7.x 处理 RF-DETR TopK 算子时的编译 bug。

### 9.4 上传项目代码（在 Windows PowerShell 中执行）

打开 PowerShell，执行：

```powershell
# 上传整个项目目录
scp -r "F:\code1.0\pig_tracking_system" root@192.168.137.100:/root/pig_tracking

# 上传 ONNX 模型（如果模型不在项目目录内）
scp "F:\...\inference_model_fixed2.onnx" root@192.168.137.100:/root/pig_tracking/
```

> 首次连接会提示确认指纹，输入 `yes` 回车即可。

### 9.5 在 Atlas 上安装 Python 依赖

SSH 登录 Atlas：

```bash
ssh root@192.168.137.100
```

安装依赖包（使用华为镜像源，速度更快）：

```bash
pip install opencv-python-headless scipy numpy -i https://repo.huaweicloud.com/repository/pypi/simple
```

验证 pyACL 是否可用（Atlas 出厂已预装）：

```bash
python3 -c "import acl; print('pyACL OK')"
```

### 9.6 重新编译 OM 模型

在 Atlas 上执行 ATC 编译（根据芯片型号选择对应命令）：

**310B1 芯片（20 TOPS，11.3GB）：**

```bash
atc --model=/root/pig_tracking/inference_model_fixed2.onnx \
    --framework=5 \
    --output=/root/pig_tracking/inference_model_310B1 \
    --soc_version=Ascend310B1 \
    --fusion_switch_file=/root/pig_tracking/fusion_switch.cfg \
    --input_shape="input:1,3,560,560" \
    --log=warning
```

**310B4 芯片（8 TOPS，4GB）：**

```bash
atc --model=/root/pig_tracking/inference_model_fixed2.onnx \
    --framework=5 \
    --output=/root/pig_tracking/inference_model_310B4 \
    --soc_version=Ascend310B4 \
    --fusion_switch_file=/root/pig_tracking/fusion_switch.cfg \
    --input_shape="input:1,3,560,560" \
    --log=warning
```

编译成功后会生成对应的 `.om` 文件。

### 9.7 配置开机自动网络（eth0 摄像头网络）

> systemd 服务方案在部分 Atlas 设备上因日志未记录导致排查困难，推荐使用更稳定的 **rc.local** 方案。

```bash
cat > /etc/rc.local << 'EOF'
#!/bin/bash
sleep 5
/usr/sbin/ip addr flush dev eth0
/usr/sbin/ip addr add 192.168.2.200/24 dev eth0
/usr/sbin/ip link set eth0 up
exit 0
EOF

chmod +x /etc/rc.local
```

启用并启动：

```bash
systemctl enable rc-local
systemctl start rc-local
systemctl status rc-local
```

验证摄像头连通性：

```bash
ping -c 3 192.168.2.103
```

重启后验证是否自动生效：

```bash
reboot
# 重新登录后
ping -c 3 192.168.2.103
```

### 9.8 验证整体部署

```bash
# 确认项目文件存在
ls /root/pig_tracking/

# 确认 OM 模型存在
ls /root/pig_tracking/*.om

# 运行一段本地视频验证推理正常（需先上传测试视频）
cd /root/pig_tracking
python deploy/atlas_pipeline.py \
    --rtsp /root/pig_tracking/片段/视频文件名.mp4 \
    --om /root/pig_tracking/inference_model_310B1.om \
    --output /root/pig_tracking/output \
    --no_video \
    --max_frames 100
```

### 9.9 常用 SCP 命令参考

```powershell
# Windows → Atlas（上传单个文件）
scp "本地文件路径" root@192.168.137.100:/root/pig_tracking/

# Windows → Atlas（上传整个目录）
scp -r "本地目录路径" root@192.168.137.100:/root/pig_tracking/

# Atlas → Windows（下载结果）
scp -r root@192.168.137.100:/root/pig_tracking/output "F:\atlas_results\"
```
